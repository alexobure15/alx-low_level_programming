#include "main.h"
#include <stdio.h>

/**
 * _putchar - writes the character c to stdout
 *
 * @c:character to print
 *
 * Return: On success 1 and  On error, -1 is
 * returned and errno is set
 */

	int _putchar(char c)
	{
		return (write(1, &c, 1));
	}
